#include <stdio.h>

int main(){
    int inputnum, totaladd, add=0; //
    scanf("%d", &inputnum);

        for(totaladd = 0; inputnum != totaladd && inputnum >= totaladd ;add++) //input >= totalnum
                totaladd += add;

    printf("%d", totaladd);
}