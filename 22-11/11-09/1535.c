#include <stdio.h>

int n,d[110];

int f(){
    int king=d[0];
    int num;

}

int main()
{
  scanf("%d", &n);

  for(int i=0; i<n; i++)
    scanf("%d", &d[i]); //arr 입력 받음 생성완료

  printf("%d", f());
  return 0;
}